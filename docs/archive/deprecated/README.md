# Deprecated Experiments

These folders are archived experiments, not the current Miguel Design OS workflow.

## Archived

- `studio/`: previous frontend Studio app experiment.
- `studio-preview/`: previous local concept preview app experiment.
- `studio-docs/`: docs for the deprecated Studio app.

## Current Workflow

Use:

```txt
rules + visual-library + prompts + screenshot QA
```

Do not build, improve, or route future work through these frontend apps unless Migi explicitly reactivates them.
